{
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": ["anonymous"],
      "backend": "baro-audio-api"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*"]
  }
}
