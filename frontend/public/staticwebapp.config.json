{
  "routes": [
    {
      "route": "/api/*",
      "allowedRoles": ["anonymous"],
      "backend": "default"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*"]
  }
}
