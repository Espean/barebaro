{
  "name": "barebaro-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@wavesurfer/react": "^1.0.11",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1",
    "wavesurfer.js": "^7.9.9"
  },
  "scripts": {
    "preinstall": "node -v && npm -v",
    "postinstall": "ls -l node_modules && npm list --depth=0",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
